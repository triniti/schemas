{
  "id": "http://schemas.triniti.io/json-schema/gdbots/ncr/mixin/get-node-batch-request/1-0-0.json#",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "properties": {
    "consistent_read": {
      "type": "boolean",
      "description": "If true, a strongly consistent read is used; if false (the default), an eventually consistent read is used.",
      "pbj": {
        "type": "boolean",
        "rule": "single"
      }
    },
    "node_refs": {
      "type": "array",
      "items": [
        {
          "type": "string",
          "pattern": "^[\\w\\/\\.:-]+$"
        }
      ],
      "uniqueItems": true,
      "additionalProperties": false,
      "description": "When \"node_refs\" is supplied it SHOULD be used to perform the request. The \"node_refs\" and \"slugs\" are analogous to protobuf unions in that only one of these should exist and the priority of selection is as ordered in this schema.",
      "pbj": {
        "type": "identifier",
        "rule": "set"
      }
    }
  },
  "additionalProperties": true
}